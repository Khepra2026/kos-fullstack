# KOS n8n Credentials
# Les fichiers de credentials n8n sont chargés ici automatiquement
# Format: JSON exporté depuis n8n UI (Settings → Credentials → Export)
# Ne pas commiter les vrais credentials dans le repo