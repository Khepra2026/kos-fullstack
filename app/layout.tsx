export const metadata = { title: 'KOS KhepraExperts', description: 'RegTech AI' };
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="fr"><body style={{margin:0,fontFamily:'system-ui'}}>{children}</body></html>;
}
