{
  "name": "kos-fullstack-enterprise",
  "version": "3.0.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo 'No build step required for backend runtime'",
    "lint": "node --check server.js && echo 'lint ok - no eslint for express minimal'",
    "test": "node --check server.js && echo 'test ok - syntax check'",
    "typecheck": "node --check server.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.8",
    "cors": "^2.8.6",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
